# Hybrid Prime: nvidia-drm KMS so X can add an NVIDIA GPU screen for offload.
# Do not set NVreg_PreserveVideoMemoryAllocations (breaks Optimus).
options nvidia-drm modeset=1
blacklist nouveau
options nouveau modeset=0
