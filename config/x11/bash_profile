# Login shell. Source .profile (PATH, umask).
# Auto-startx is off: hybrid graphics / XLibre failures were logging
# the user straight back out (exec startx replaces the shell).
[ -f "$HOME/.profile" ] && . "$HOME/.profile"

# To start the desktop from tty1 after login:
#   startx
# Logs: ~/.local/share/xorg/Xorg.0.log  or  /var/log/Xorg.0.log
