# Login shell. Source .profile (PATH, umask) then auto-startx on tty1 only.
[ -f "$HOME/.profile" ] && . "$HOME/.profile"

# SARTORIA_AUTO_STARTX
if [ -z "$DISPLAY" ] && [ "$(tty 2>/dev/null)" = "/dev/tty1" ]; then
  exec startx
fi
