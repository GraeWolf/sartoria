# Hybrid laptop: the panel is on the iGPU (10-igpu.conf, PrimaryGPU yes).
# NVIDIA has no internal outputs. Keep it as a Prime offload source.
# Do not set PrimaryGPU on nvidia (that is reverse-PRIME / dGPU-drives-panel).
#
# AutoAddGPU must be false. With nvidia-drm KMS, XLibre modesetting otherwise
# claims /dev/dri/card1 as modeset(G0) and the nvidia DDX also claims it as
# NVIDIA(G1). Then: "Failed to acquire modesetting permission" / drmSetMaster
# denied / AddScreen failed. AllowNVIDIAGPUScreens still lets the nvidia DDX
# add the offload GPU screen.
Section "OutputClass"
    Identifier "nvidia-hybrid-offload"
    MatchDriver "nvidia-drm"
    Driver "nvidia"
    Option "AllowEmptyInitialConfiguration" "true"
    Option "PrimaryGPU" "no"
    ModulePath "/usr/lib/x86_64-linux-gnu/nvidia/xorg,/usr/lib/nvidia/nvidia"
EndSection

Section "ServerFlags"
    Option "AutoAddGPU" "false"
    Option "AllowNVIDIAGPUScreens" "true"
EndSection
