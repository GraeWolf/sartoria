# Minutes with no keyboard or pointer input before the console locks.
# 0 turns the idle timer off. Super+Shift+Esc and lid close still lock.
# ~/.config/sartoria/lock.conf overrides this file.
# SARTORIA_LOCK_MINUTES overrides both.
minutes=5
